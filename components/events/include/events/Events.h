#pragma once

#include "esp_event.h"

ESP_EVENT_DECLARE_BASE(BUTTON_EVENTS);


enum {
    BUTTON_PRESSED_EVENT,
    BUTTON_RELEASED_EVENT
};

ESP_EVENT_DECLARE_BASE(BLUETOOTH_EVENTS);

enum {
    BLUETOOTH_MESSAGE_RECEIVED_EVENT
};

